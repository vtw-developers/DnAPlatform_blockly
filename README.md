# DnA Platform - Blockly 기반 비주얼 프로그래밍 플랫폼

## 📖 프로젝트 소개

**DnA Platform**은 Google Blockly를 기반으로 한 비주얼 프로그래밍 환경입니다. 블록을 드래그 앤 드롭하여 직관적으로 Python 코드를 작성하고, AI를 활용한 코드 검증 및 JavaScript 변환 기능을 제공합니다.

---

## 🧩 주요 기능

### 1. 비주얼 프로그래밍 (Blockly)

- **드래그 앤 드롭 인터페이스**: 코드 블록을 끌어다 놓으며 프로그램 작성
- **실시간 코드 생성**: 블록 배치와 동시에 Python 코드로 자동 변환
- **다양한 블록 카테고리**:
  - 논리 블록 (조건문, 반복문)
  - 수학 블록 (연산자, 함수)
  - 텍스트 블록 (문자열 처리)
  - 리스트 블록 (배열 조작)
  - 변수 블록 (변수 선언 및 사용)
  - 함수 블록 (사용자 정의 함수)
  - 커스텀 블록 (프로젝트 특화 블록)

### 2. 코드 저장 및 관리

- **프로젝트 저장**: 블록 워크스페이스를 프로젝트로 저장
- **불러오기**: 저장된 프로젝트를 다시 불러와 편집
- **공유**: 다른 사용자와 프로젝트 공유 가능

### 3. AI 코드 검증

- **자동 검증**: 작성한 코드의 정확성을 AI가 분석
- **모델 선택**: Ollama 또는 OpenAI 모델 선택 가능
- **결과 리포트**: 검증 결과 및 개선 제안 제공

### 4. 코드 변환 (Python → JavaScript)

- **자동 변환**: Python 코드를 JavaScript로 변환
- **변환 규칙 관리**: 커스텀 변환 규칙 추가 및 수정
- **AI 규칙 생성**: AI가 자동으로 변환 규칙 생성

---

## 🚀 빠른 시작

### 설치 및 실행

```bash
# 저장소 클론
git clone https://github.com/vtw-developers/DnAPlatform_blockly.git
cd DnAPlatform_blockly

# 환경 변수 설정
cp .env.example .env

# Docker Compose로 실행
docker-compose up -d
```

### 접속

- **플랫폼**: http://localhost:5050
- **초기 계정**: admin / admin123

---

## 💻 Blockly 사용 방법

### 1. 블록 프로그래밍 시작

```
1. 좌측 툴박스에서 원하는 블록 선택
2. 작업 영역(워크스페이스)으로 드래그
3. 블록끼리 연결하여 로직 구성
4. 우측 패널에서 생성된 코드 확인
```

### 2. 블록 카테고리

#### 📊 논리 블록
- `if-else`: 조건문
- `while`: 반복문 (조건 기반)
- `for`: 반복문 (범위 기반)

#### 🔢 수학 블록
- 사칙연산: `+`, `-`, `*`, `/`
- 비교 연산: `>`, `<`, `==`
- 수학 함수: `abs()`, `max()`, `min()`

#### 📝 텍스트 블록
- 문자열 생성 및 결합
- 문자열 길이, 대소문자 변환
- 문자열 검색 및 치환

#### 📦 리스트 블록
- 리스트 생성: `[]`
- 요소 추가: `append()`
- 요소 접근: `list[index]`

#### 🔧 변수 블록
- 변수 선언 및 값 할당
- 변수 값 변경
- 변수 사용

#### ⚡ 함수 블록
- 함수 정의
- 매개변수 설정
- 반환값 지정

### 3. 프로젝트 저장

```
1. 상단 메뉴에서 "저장" 클릭
2. 프로젝트 이름 입력
3. 저장 완료 → 프로젝트 목록에서 확인
```

### 4. 프로젝트 불러오기

```
1. 상단 메뉴에서 "불러오기" 클릭
2. 프로젝트 목록에서 선택
3. 블록 워크스페이스에 자동 로드
```

---

## 🎨 커스텀 블록 추가 (개발자용)

Blockly 블록은 TypeScript로 정의됩니다.

### 블록 정의 예시

```typescript
// frontend/src/components/blockly/customBlocks/myBlocks.ts

import * as Blockly from 'blockly/core';

// 블록 구조 정의
export const customPrintBlock = {
  type: 'custom_print',
  message0: '출력하기 %1',
  args0: [
    {
      type: 'input_value',
      name: 'TEXT'
    }
  ],
  previousStatement: null,
  nextStatement: null,
  colour: 160,
  tooltip: '값을 출력합니다',
};

// 블록 초기화
Blockly.Blocks['custom_print'] = {
  init: function() {
    this.jsonInit(customPrintBlock);
  }
};

// Python 코드 생성기
Blockly.Python['custom_print'] = function(block) {
  const text = Blockly.Python.valueToCode(
    block, 'TEXT', Blockly.Python.ORDER_ATOMIC
  );
  return `print(${text})\n`;
};
```

### 블록 등록

```typescript
// frontend/src/components/blockly/BlocklyComponent.tsx

import { customPrintBlock } from './customBlocks/myBlocks';

// 블록을 Blockly 워크스페이스에 등록
const toolbox = {
  kind: 'categoryToolbox',
  contents: [
    {
      kind: 'category',
      name: '내 블록',
      colour: 160,
      contents: [
        { kind: 'block', type: 'custom_print' }
      ]
    }
  ]
};
```